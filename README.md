# ivana-design-legacy

Tema de Tiendanube para `ivanadesign.com.ar`.

## Regla operativa

- La unica rama de trabajo y publicacion es `main`.
- Los cambios se commitean en `main` y se publican con `git push origin main`.
- Este repositorio no mantiene un flujo activo en ramas paralelas para el theme.
